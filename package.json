{
  "name": "background-music-site",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "deploy": "npm run build && aws s3 sync dist/ s3://andrewlastrapes.com --delete && aws cloudfront create-invalidation --distribution-id ETY7T2HGL5CBE --paths '/*'",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^5.6.0"
  },
  "devDependencies": {
    "@astrojs/tailwind": "^6.0.2",
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.49",
    "tailwindcss": "^3.4.17"
  }
}